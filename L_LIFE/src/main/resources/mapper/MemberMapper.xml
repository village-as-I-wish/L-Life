<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kosa.com.suntofu.L_LIFE.member.mapper.MemberMapper">

    <!-- 정보 찾기 -->
<!--    <select id="findKakao" parameterType="java.util.HashMap" resultType="kdto">-->
<!--        select * from member where m_name=#{nickname} and m_email=#{email}-->
<!--    </select>-->

    <!-- 정보 저장 -->
<!--    <insert id="kakaoInsert" parameterType="java.util.HashMap">-->
<!--        insert into member(m_name, m_email)-->
<!--        values(#{nickname}, #{email})-->
<!--    </insert>-->

    <select id="insertOrGetUser" statementType="CALLABLE" resultType="java.util.Map">
        {call insert_or_get_user(#{inputValue, mode=IN, jdbcType=VARCHAR},
                                 #{inputType, mode=IN, jdbcType=VARCHAR},
                                 #{userId, mode=OUT, jdbcType=VARCHAR})}
    </select>

<!--    <select id="selectOneMember" resultType="MemberVo">-->
<!--		<![CDATA[-->
<!--        SELECT * FROM "member" WHERE "USER_ID"={userId}-->
<!--        ]]>-->
<!--	</select>-->


</mapper>