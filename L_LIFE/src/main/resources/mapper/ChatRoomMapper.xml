<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kosa.com.suntofu.L_LIFE.chat.dao.ChatRoomDao">

<!--    <select id="findAllRooms">-->
<!--        SELECT * FROM LIVESTREAM-->
<!--    </select>-->

    <select id="findRoomById" parameterType="int">
        SELECT * FROM LIVESTREAM WHERE L_STREAM_ID = #{lStreamId}
    </select>

</mapper>